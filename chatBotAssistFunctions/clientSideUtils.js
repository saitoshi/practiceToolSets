/**
 * @name clientSideUtils.js 
 * @desc Functions written for the client side of the chat bot to manage UX
 */

/**
 * @name controlWordCount 
 * @desc If the current input exceeds 200 characters disable the send button 
 * @return modifies the send button 
 */
const controlWordCount = () => {
    const sendButtonID = document.getElementById("") ;
    const maxMessageCount = 201;
    const inputAreaID = document.getElementById();
    inputAreaID.addEventListener("input", function () {
   
    })
   
}

/**
 * @name 
 */